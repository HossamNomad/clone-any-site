# Node
node_modules/
npm-debug.log*
package-lock.json

# Runtime artifacts the skill PRODUCES (must never be committed — see the IP rule in README)
mirror/
**/mirror/
_vendor/
_recon/fidelity/
.fidelity/
fidelity-report.md
*.crawl-log.json

# Repurpose Layer — the numbered manifest is tracked, but its payloads are user content / IP and never shipped
repurpose/assets/
repurpose/preview/
repurpose/build/
**/repurpose/assets/
**/repurpose/preview/
**/repurpose/build/

# Test scratch (the suite writes here; fixtures + .test.mjs ARE tracked)
**/test/.work/

# Local scratch
*.tmp
.gitignore.copy

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
